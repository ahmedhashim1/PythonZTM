# short circuiting
# is_friend = True
# is_user = False
#
# if is_friend or is_user:  # using or in this scenario is more efficient, because python will not look for next logic
#     print("You are a best friend")

# print('a' > 'A')
# print(1 < 2 < 3 < 4 < 5)  # all true so no shortciruiting
# print(1 < 2 < 3 > 4 < 5)  # 1 equation false, so shortciruited
